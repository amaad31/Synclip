# Synclip

Lightweight, encrypted file sync & clipboard daemon, inotify-based delta sync with AES-256-GCM, chunked parallel uploads, and P2P local clipboard sharing via mDNS.

**Detailed Description is to come**
